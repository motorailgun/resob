pub mod module;
pub mod section;
pub mod types;

/* #[derive(Eq, PartialEq, Debug)]
struct Type {
    pub size: usize,
    pub inner: Vec<u8>
}
#[derive(Eq, PartialEq, Debug)]
struct Code {
    pub size: usize,
    pub inner: Vec<u8>
}
#[derive(Eq, PartialEq, Debug)]
struct Function {
    pub size: usize,
    pub inner: Vec<u8>
}
#[derive(Eq, PartialEq, Debug)]
struct Memory {
    pub size: usize,
    pub inner: Vec<u8>
}
#[derive(Eq, PartialEq, Debug)]
struct Data {
    pub size: usize,
    pub inner: Vec<u8>
}
#[derive(Eq, PartialEq, Debug)]
struct Export {
    pub size: usize,
    pub inner: Vec<u8>
}
#[derive(Eq, PartialEq, Debug)]
struct Import {
    pub size: usize,
    pub inner: Vec<u8>
}


#[derive(Eq, PartialEq, Debug)]
enum Section {
    Type(Type),
    Import(Import),
    Function(Function),
    Memory(Memory),
    Export(Export),
    Code(Code),
    Data(Data),
} */
